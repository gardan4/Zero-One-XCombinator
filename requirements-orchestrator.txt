# Light deps for zo-cluster / judge CLIs on a laptop (no uv, no GPU stack).
# Judges: pip only — uv is NOT required.
#
#   python -m pip install -r requirements-orchestrator.txt
#   python scripts/zo_cluster.py judge-eval --dry-run --no-stage --eval-dir extras/eval_local
#
# Optional: uv run zo-cluster …  (if uv is installed)

pydantic>=2.7
pyyaml>=6
typer>=0.12
rich>=13
jinja2>=3
httpx>=0.27
