# Laptop orchestrator for Leonardo smoke finetune (scripts/leonardo_smoke.py).
# Judges: pip only — uv is NOT required. Training runs on Leonardo.
#
#   python -m pip install -r requirements-orchestrator.txt
#   python scripts/leonardo_smoke.py --dry-run
#
# Optional: uv run python scripts/leonardo_smoke.py  (if uv is installed)

pydantic>=2.7
pyyaml>=6
typer>=0.12
rich>=13
jinja2>=3
httpx>=0.27
